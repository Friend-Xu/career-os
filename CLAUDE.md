@AGENTS.md

# 开发原则

1. **高内聚**：一个模块/文件职责单一，相关逻辑聚合在所属模块内，不散落到无关文件。
2. **低耦合**：模块间通过稳定接口交互，不互相渗透实现细节；改一个模块不影响无关模块。
3. **单向依赖**：依赖方向自上而下单向（页面 → store → 数据层 → 常量），禁止反向依赖与循环依赖。
4. **禁止兜底**：不为不可能发生的场景写 fallback/兜底/防御逻辑；信任内部契约，仅在系统边界（用户输入、外部 API）做校验。
5. **环境隔离**：所有语言运行时与依赖安装**必须在项目根目录内**（如 `.local/`、`node_modules/`），禁止在外部环境安装任何依赖/包。npm 依赖默认即项目内安装（`package.json` 声明 → 项目内 `node_modules/`，合规）；**`npm install -g` / 全局 pip / 系统包管理器 / 系统 PATH 修改一律禁止——篡改外部环境是严重的系统级错误**；npm 下载缓存（`~/.npm`）只是缓存，不算环境。Python 运行时使用 `D:\Workspace\Python便携版\python-3.10.9-embed-amd64.zip`（解压进项目内使用，不加入 PATH、不装全局 pip 包）。
