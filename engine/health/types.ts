/**
 * HealthReport 契约类型（re-export 契约源 ir/schema.ts）。
 * 类型定义以 ir/schema.ts 为唯一权威（引擎 ↔ UI 共享契约源）。
 */
export type { DimensionName, HealthDimension, HealthIssue, HealthReport } from '../ir/schema.ts'
export type { PersonHealth, PersonHealthCheck } from '../ir/schema.ts'
