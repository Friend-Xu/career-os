{
  "name": "career-os-engine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Career OS 本地引擎（纯 Node 服务，零依赖，Node 24 原生 TS type-stripping）",
  "scripts": {
    "engine": "node main.ts",
    "test": "node --test --test-isolation=none \"tests/*.test.ts\""
  },
  "engines": {
    "node": ">=24"
  }
}
